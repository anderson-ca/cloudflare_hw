<div class="container">
    <div class="row">
        <div class="card">
            <div class="card-header">
                Headers
            </div>
            <ul class="list-group list-group-flush">
                {{#each headers}}
                <li class="list-group-item">{{@key}}: {{this}}</li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>